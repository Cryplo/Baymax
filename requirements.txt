# Baymax - Required Dependencies
# Install with: pip install -r requirements.txt

# CLI Framework
typer>=0.9.0
rich>=13.0.0

# Environment Configuration
python-dotenv>=1.0.0

# Claude Agent SDK (requires Claude Code CLI installed separately)
claude-agent-sdk>=0.1.0
